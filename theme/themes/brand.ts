import { vars } from "nativewind";

export const brand = {
  'light': vars({
    '--color-input': 'red',
    '--color-primary': 'red',
    '--color-secondary': 'green'
  }),
  'dark': vars({
    '--color-input': 'green',
    '--color-primary': 'green',
    '--color-secondary': 'red'
  })
}